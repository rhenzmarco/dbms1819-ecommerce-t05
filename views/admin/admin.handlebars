<h1>&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;&ensp;WELCOME TO ADMIN PAGE<h1>
    <div class="container">
    <div class="jumbotron mt-2">
    <h3 align="center">Dashboard</h3><hr>
        <div class="row">
            <div class="col-md-5" style="margin: 0 40px">
            <h5 align="center">Top 10 Customers with Most No. of Orders</h5>
                <div class="row justify-content-center">
                    <table class="table text-center">
                        <thead>
                            <tr>
                                <th>Customer</th>
                                <th>Number of Orders</th>
                             </tr>
                        </thead>
                        <tbody>
                            {{#each topCustomersMostOrder}}
                            <tr>
                                <td>{{this.first_name}}  {{this.last_name}}</td>
                                <td>{{this.count}}</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col-md-5" style="margin: 0 40px">
            <h5 align="center">Top 10 Customers with Highest Payment</h5>
                <div class="row justify-content-center">
                    <table class="table text-center">
                        <thead>
                            <tr>
                                <th>Customer</th>
                                <th>Total Payment</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each topCustomersHighestPayment}}
                            <tr>
                                <td align="left">{{this.first_name}}  {{this.last_name}}</td>
                                <td align="left">Php {{ this.sum}}.00</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>